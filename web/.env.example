DATABASE_URL="postgres://postgres:password@localhost:5432/postgres"
SESSION_SECRET="your-session-secret"
MAGIC_LINK_SECRET="your-magic-link-secret"
CLIENT_URL='http://localhost:5173'
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
RESEND_API_KEY="your-resend-api-key"

# GitHub Configuration for markdown sync
# Note: Only users with provider="Admin" can access the sync feature

# 1. GITHUB_TOKEN: Create at https://github.com/settings/tokens
#    - Go to GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
#    - Generate new token with 'repo' scope
GITHUB_TOKEN="your-github-personal-access-token"

# 2. For repository https://github.com/owner-name/repo-name:
#    GITHUB_OWNER would be "owner-name"
#    GITHUB_REPO would be "repo-name"
GITHUB_OWNER="your-github-username"
GITHUB_REPO="your-repo-name"
